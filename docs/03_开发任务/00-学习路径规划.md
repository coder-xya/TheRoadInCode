# 学习路径规划

> **适用对象**：初级前端开发者（熟悉 JavaScript，其他技术栈不熟悉）
> **学习方式**：边做边学，每个阶段有明确的学习目标和实践任务
> **项目目标**：通过构建个人博客系统，系统学习现代全栈开发技术

---

## 学习原则

1. **小步快跑**：每个任务都能看到成果，保持学习动力
2. **实践优先**：先动手做，遇到问题再学习理论
3. **循序渐进**：从简单到复杂，逐步增加难度
4. **及时反馈**：每完成一个任务都要验证效果

---

## 阶段总览

| 阶段 | 名称               | 学习重点             | 预计时间 |
| ---- | ------------------ | -------------------- | -------- |
| P0   | 环境搭建与项目认知 | Git、pnpm、项目结构  | 1-2 天   |
| P1   | TypeScript 基础    | 类型系统、接口、泛型 | 3-5 天   |
| P2   | React 与 Next.js   | 组件、Hooks、路由    | 5-7 天   |
| P3   | 样式与布局         | TailwindCSS、响应式  | 3-5 天   |
| P4   | 后端入门           | NestJS、API 设计     | 5-7 天   |
| P5   | 数据库操作         | Prisma、PostgreSQL   | 3-5 天   |
| P6   | 前后端联调         | API 调用、状态管理   | 5-7 天   |
| P7   | 功能完善           | 认证、CRUD 完整流程  | 7-14 天  |

---

## P0: 环境搭建与项目认知

### 学习目标

- [ ] 理解 Monorepo 项目结构
- [ ] 掌握 pnpm 基本命令
- [ ] 熟悉 Git 基本操作
- [ ] 成功运行项目

### 任务清单

#### 任务 0.1：理解项目结构

**阅读并理解以下目录的作用**：

```
TheRoadInCode/
├── apps/                    # 应用目录
│   ├── web/                 # Next.js 前端应用
│   └── api/                 # NestJS 后端应用
├── packages/                # 共享包
│   ├── database/            # Prisma 数据库配置
│   ├── shared/              # 共享类型和工具
│   ├── typescript-config/   # TypeScript 配置
│   └── eslint-config/       # ESLint 配置
├── docs/                    # 项目文档
├── docker/                  # Docker 配置
├── package.json             # 根目录配置
├── pnpm-workspace.yaml      # pnpm 工作空间配置
└── turbo.json               # Turborepo 配置
```

**需要理解的概念**：

- Monorepo：多个项目放在一个仓库中管理
- Workspace：pnpm/npm 的工作空间功能，共享依赖
- Turborepo：构建系统，管理多项目的构建和开发

**学习资源**：

- [pnpm 官方文档](https://pnpm.io/zh/motivation)
- [Turborepo 官方文档](https://turborepo.dev/docs)

#### 任务 0.2：启动数据库

**步骤**：

1. 确保 Docker Desktop 已安装并运行
2. 启动 PostgreSQL：
   ```bash
   cd docker
   docker-compose up -d
   ```
3. 验证：
   ```bash
   docker ps
   # 应该看到 postgres 容器在运行
   ```

**如果你没有安装 Docker**：

- Windows: [Docker Desktop 下载](https://www.docker.com/products/docker-desktop/)
- 安装后重启电脑

#### 任务 0.3：初始化数据库

**步骤**：

1. 进入 database 包目录：

   ```bash
   cd packages/database
   ```

2. 创建环境变量文件：

   ```bash
   # 创建 .env 文件，内容如下：
   DATABASE_URL="postgresql://postgres:postgres@localhost:5432/theroadincode_dev?schema=public"
   ```

3. 生成 Prisma Client：

   ```bash
   pnpm db:generate
   ```

4. 推送数据库结构：

   ```bash
   pnpm db:push
   ```

5. 打开 Prisma Studio 查看数据库：
   ```bash
   pnpm db:studio
   ```

   - 浏览器会打开 http://localhost:5555
   - 你应该能看到数据库中的表

#### 任务 0.4：启动项目

**步骤**：

1. 回到项目根目录：

   ```bash
   cd ../..
   ```

2. 配置后端环境变量：

   ```bash
   # apps/api/.env.local
   DATABASE_URL="postgresql://postgres:postgres@localhost:5432/theroadincode_dev?schema=public"
   NODE_ENV=development
   PORT=4000
   CORS_ORIGIN=http://localhost:3000
   JWT_SECRET=your-super-secret-key-change-in-production
   JWT_REFRESH_SECRET=your-refresh-secret-key-change-in-production
   ```

3. 配置前端环境变量：

   ```bash
   # apps/web/.env.local
   NEXT_PUBLIC_API_URL=http://localhost:4000/api/v1
   ```

4. 启动开发服务器：

   ```bash
   pnpm dev
   ```

5. 验证：
   - 前端：http://localhost:3000 看到首页
   - 后端：http://localhost:4000/api/v1/health 看到健康检查响应

### 验收标准

- ✅ 能解释 apps、packages 目录的作用
- ✅ Docker 容器运行正常
- ✅ Prisma Studio 可以打开
- ✅ 前后端都能正常启动

### 常见问题

**Q: pnpm 命令报错**
A: 确保 Node.js >= 20，运行 `node -v` 检查版本

**Q: Docker 启动失败**
A: 确保 Docker Desktop 已启动，检查是否有其他程序占用 5432 端口

**Q: Prisma generate 失败**
A: 检查 DATABASE_URL 是否正确，数据库是否已启动

---

## P1: TypeScript 基础

### 学习目标

- [ ] 理解类型注解的作用
- [ ] 掌握基本类型（string, number, boolean, array, object）
- [ ] 理解 interface 和 type 的区别
- [ ] 能读懂项目中的 TypeScript 代码

### 前置知识

你需要熟悉 JavaScript 的以下概念：

- 变量声明（let, const）
- 函数（箭头函数）
- 对象和数组
- 解构赋值
- 模块导入导出（import/export）

### 任务清单

#### 任务 1.1：类型注解入门

**在项目中创建练习文件**：

```bash
# 创建练习目录（不会提交到 Git）
mkdir -p apps/web/src/practice
```

**创建练习文件** `apps/web/src/practice/01-basics.ts`：

```typescript
// 练习 1：基本类型
// 尝试修改这些变量的值，观察 TypeScript 的类型检查

// 字符串
let title: string = '我的博客';
// title = 123;  // 取消注释，看看会发生什么

// 数字
let viewCount: number = 100;

// 布尔值
let isPublished: boolean = true;

// 数组
let tags: string[] = ['TypeScript', 'React', 'Next.js'];

// 对象
let post: {
  title: string;
  content: string;
  views: number;
} = {
  title: '第一篇文章',
  content: '这是内容',
  views: 0,
};

// 练习：尝试给 post 添加一个不存在的属性
// post.author = "me";  // 取消注释，看看会发生什么

console.log(title, viewCount, isPublished, tags, post);
```

**运行方式**：在 VSCode/Cursor 中打开文件，鼠标悬停在变量上可以看到类型信息

#### 任务 1.2：Interface 和 Type

**创建练习文件** `apps/web/src/practice/02-interface.ts`：

```typescript
// 练习 2：Interface（接口）
// Interface 用于定义对象的结构

// 定义文章接口
interface Post {
  id: string;
  title: string;
  content: string;
  summary?: string; // ? 表示可选属性
  views: number;
  isPublished: boolean;
  createdAt: Date;
}

// 使用接口
const myPost: Post = {
  id: '1',
  title: '学习 TypeScript',
  content: 'TypeScript 是 JavaScript 的超集...',
  // summary 是可选的，可以不写
  views: 100,
  isPublished: true,
  createdAt: new Date(),
};

// 定义作者接口
interface Author {
  id: string;
  name: string;
  email: string;
}

// 接口可以扩展
interface PostWithAuthor extends Post {
  author: Author;
}

// 练习：创建一个 PostWithAuthor 类型的对象
const postWithAuthor: PostWithAuthor = {
  ...myPost,
  author: {
    id: 'author-1',
    name: '张三',
    email: 'zhangsan@example.com',
  },
};

console.log(postWithAuthor);
```

#### 任务 1.3：阅读项目中的类型定义

**查看项目中的真实类型定义**：

1. 打开 `packages/shared/src/types.ts`
2. 阅读并理解以下类型：
   - `User` - 用户类型
   - `Post` - 文章类型
   - `ApiResponse` - API 响应类型
   - `PaginatedResponse` - 分页响应类型

3. 思考问题：
   - 为什么要定义这些类型？
   - 前后端如何共享这些类型？

#### 任务 1.4：函数类型

**创建练习文件** `apps/web/src/practice/03-functions.ts`：

```typescript
// 练习 3：函数类型

// 基本函数类型
function greet(name: string): string {
  return `Hello, ${name}!`;
}

// 箭头函数
const add = (a: number, b: number): number => {
  return a + b;
};

// 可选参数
function createPost(title: string, content?: string): { title: string; content: string } {
  return {
    title,
    content: content || '暂无内容',
  };
}

// 默认参数
function formatDate(date: Date, locale: string = 'zh-CN'): string {
  return date.toLocaleDateString(locale);
}

// 异步函数
async function fetchPost(id: string): Promise<{ id: string; title: string }> {
  // 模拟 API 调用
  return { id, title: '示例文章' };
}

// 练习：调用这些函数
console.log(greet('TypeScript'));
console.log(add(1, 2));
console.log(createPost('我的文章'));
console.log(formatDate(new Date()));
```

### 学习资源

- [TypeScript 官方手册（中文）](https://www.typescriptlang.org/zh/docs/handbook/2/basic-types.html)
- [TypeScript 入门教程](https://ts.xcatliu.com/)

### 验收标准

- ✅ 能解释 `string`、`number`、`boolean` 等基本类型
- ✅ 能使用 `interface` 定义对象结构
- ✅ 能为函数添加参数和返回值类型
- ✅ 能读懂 `packages/shared/src/types.ts` 中的类型定义

---

## P2: React 与 Next.js

### 学习目标

- [ ] 理解 React 组件的概念
- [ ] 掌握 useState、useEffect 等基础 Hooks
- [ ] 理解 Next.js App Router 的路由机制
- [ ] 能创建简单的页面和组件

### 任务清单

#### 任务 2.1：理解 React 组件

**打开并阅读** `apps/web/src/components/layout/header.tsx`：

思考以下问题：

1. 组件是如何定义的？（函数组件）
2. `props` 是什么？
3. JSX 是什么？为什么可以在 JavaScript 中写 HTML？
4. 组件是如何导出的？

#### 任务 2.2：创建你的第一个组件

**创建文件** `apps/web/src/components/practice/Greeting.tsx`：

```tsx
// 这是一个简单的 React 组件

// 定义 props 类型
interface GreetingProps {
  name: string;
  showTime?: boolean; // 可选属性
}

// 组件是一个函数，接收 props，返回 JSX
export function Greeting({ name, showTime = false }: GreetingProps) {
  // 获取当前时间
  const currentTime = new Date().toLocaleTimeString('zh-CN');

  return (
    <div className="rounded-lg bg-blue-100 p-4">
      <h2 className="text-xl font-bold">你好，{name}！</h2>
      {showTime && <p className="text-gray-600">当前时间：{currentTime}</p>}
    </div>
  );
}
```

#### 任务 2.3：使用 useState Hook

**创建文件** `apps/web/src/components/practice/Counter.tsx`：

```tsx
'use client'; // Next.js 13+ 中，使用 Hooks 的组件需要标记为客户端组件

import { useState } from 'react';

export function Counter() {
  // useState 返回一个数组：[当前值, 设置值的函数]
  const [count, setCount] = useState(0);

  return (
    <div className="rounded-lg bg-green-100 p-4">
      <p className="mb-4 text-2xl font-bold">计数：{count}</p>
      <div className="space-x-2">
        <button
          onClick={() => setCount(count - 1)}
          className="rounded bg-red-500 px-4 py-2 text-white"
        >
          -1
        </button>
        <button
          onClick={() => setCount(count + 1)}
          className="rounded bg-blue-500 px-4 py-2 text-white"
        >
          +1
        </button>
        <button onClick={() => setCount(0)} className="rounded bg-gray-500 px-4 py-2 text-white">
          重置
        </button>
      </div>
    </div>
  );
}
```

#### 任务 2.4：理解 Next.js 路由

**App Router 路由规则**：

```
apps/web/src/app/
├── page.tsx           → /
├── about/
│   └── page.tsx       → /about
├── posts/
│   ├── page.tsx       → /posts
│   └── [slug]/
│       └── page.tsx   → /posts/hello-world (动态路由)
└── layout.tsx         → 所有页面共享的布局
```

**创建一个新页面** `apps/web/src/app/practice/page.tsx`：

```tsx
import { Greeting } from '@/components/practice/Greeting';
import { Counter } from '@/components/practice/Counter';

export default function PracticePage() {
  return (
    <div className="container mx-auto space-y-8 p-8">
      <h1 className="text-3xl font-bold">React 练习页面</h1>

      <section>
        <h2 className="mb-4 text-xl font-semibold">组件示例</h2>
        <Greeting name="学习者" showTime />
      </section>

      <section>
        <h2 className="mb-4 text-xl font-semibold">useState 示例</h2>
        <Counter />
      </section>
    </div>
  );
}
```

**访问** http://localhost:3000/practice 查看效果

#### 任务 2.5：理解服务端组件和客户端组件

Next.js 13+ 有两种组件：

| 类型       | 标记           | 运行环境 | 可以使用                       |
| ---------- | -------------- | -------- | ------------------------------ |
| 服务端组件 | 默认           | 服务器   | 数据获取、访问数据库           |
| 客户端组件 | `"use client"` | 浏览器   | useState、事件处理、浏览器 API |

**规则**：

- 默认都是服务端组件
- 需要用户交互（点击、输入）的组件标记为 `"use client"`
- 客户端组件可以导入服务端组件，反过来不行

### 学习资源

- [React 官方文档](https://react.dev/learn)
- [Next.js 官方教程](https://nextjs.org/learn)

### 验收标准

- ✅ 能创建一个接收 props 的组件
- ✅ 能使用 useState 管理组件状态
- ✅ 理解 `"use client"` 的作用
- ✅ 能创建新的页面路由

---

## P3: 样式与布局

### 学习目标

- [ ] 掌握 TailwindCSS 基本用法
- [ ] 能实现响应式布局
- [ ] 理解 Flex 和 Grid 布局

### 任务清单

#### 任务 3.1：TailwindCSS 基础

**TailwindCSS 是什么**：

- 一个"功能类优先"的 CSS 框架
- 不写 CSS 文件，直接在 HTML/JSX 中使用类名
- 例如：`className="text-xl font-bold text-blue-500"`

**常用类名速查**：

```
// 文字
text-sm / text-base / text-lg / text-xl / text-2xl  // 字号
font-normal / font-medium / font-bold               // 字重
text-gray-500 / text-blue-500 / text-red-500        // 颜色

// 间距
p-4 / px-4 / py-4 / pt-4 / pb-4 / pl-4 / pr-4      // padding
m-4 / mx-4 / my-4 / mt-4 / mb-4 / ml-4 / mr-4      // margin
space-x-4 / space-y-4                               // 子元素间距

// 布局
flex / grid                                         // 布局模式
flex-col / flex-row                                 // flex 方向
items-center / justify-center                       // 对齐
gap-4                                               // 间隙

// 尺寸
w-full / w-1/2 / w-64                              // 宽度
h-full / h-screen / h-64                           // 高度

// 背景和边框
bg-white / bg-gray-100 / bg-blue-500               // 背景色
rounded / rounded-lg / rounded-full                 // 圆角
border / border-2 / border-gray-300                // 边框
```

#### 任务 3.2：创建文章卡片组件

**创建文件** `apps/web/src/components/practice/PostCard.tsx`：

```tsx
interface PostCardProps {
  title: string;
  summary: string;
  date: string;
  tags: string[];
}

export function PostCard({ title, summary, date, tags }: PostCardProps) {
  return (
    <article className="rounded-lg border p-6 transition-shadow hover:shadow-lg">
      {/* 标题 */}
      <h3 className="mb-2 text-xl font-bold hover:text-blue-600">{title}</h3>

      {/* 摘要 */}
      <p className="mb-4 line-clamp-2 text-gray-600">{summary}</p>

      {/* 底部信息 */}
      <div className="flex items-center justify-between">
        {/* 标签 */}
        <div className="flex gap-2">
          {tags.map((tag) => (
            <span key={tag} className="rounded bg-gray-100 px-2 py-1 text-sm text-gray-600">
              {tag}
            </span>
          ))}
        </div>

        {/* 日期 */}
        <span className="text-sm text-gray-400">{date}</span>
      </div>
    </article>
  );
}
```

#### 任务 3.3：响应式布局

**响应式断点**：

```
sm:  // >= 640px  (手机横屏)
md:  // >= 768px  (平板)
lg:  // >= 1024px (笔记本)
xl:  // >= 1280px (桌面)
2xl: // >= 1536px (大屏)
```

**创建响应式文章列表** `apps/web/src/components/practice/PostList.tsx`：

```tsx
import { PostCard } from './PostCard';

const mockPosts = [
  {
    title: '学习 TypeScript 的第一天',
    summary: 'TypeScript 是 JavaScript 的超集，它添加了类型系统和其他特性...',
    date: '2026-01-20',
    tags: ['TypeScript', '学习笔记'],
  },
  {
    title: 'React Hooks 完全指南',
    summary: 'Hooks 是 React 16.8 引入的新特性，让你在函数组件中使用状态和其他 React 特性...',
    date: '2026-01-18',
    tags: ['React', 'Hooks'],
  },
  {
    title: 'Next.js App Router 入门',
    summary: 'Next.js 13 引入了全新的 App Router，基于 React Server Components...',
    date: '2026-01-15',
    tags: ['Next.js', 'React'],
  },
];

export function PostList() {
  return (
    <div className="grid grid-cols-1 gap-6 md:grid-cols-2 lg:grid-cols-3">
      {mockPosts.map((post, index) => (
        <PostCard key={index} {...post} />
      ))}
    </div>
  );
}
```

**更新练习页面**：

```tsx
// apps/web/src/app/practice/page.tsx
import { PostList } from '@/components/practice/PostList';

export default function PracticePage() {
  return (
    <div className="container mx-auto p-8">
      <h1 className="mb-8 text-3xl font-bold">文章列表</h1>
      <PostList />
    </div>
  );
}
```

### 学习资源

- [TailwindCSS 官方文档](https://tailwindcss.com/docs)
- [Tailwind Play](https://play.tailwindcss.com/) - 在线练习

### 验收标准

- ✅ 能使用 TailwindCSS 常用类名
- ✅ 能实现响应式布局（不同屏幕宽度显示不同列数）
- ✅ 创建了美观的文章卡片组件

---

## P4-P7: 后续阶段预览

### P4: 后端入门

- NestJS 基础概念（模块、控制器、服务）
- 创建简单的 API 端点
- 理解装饰器语法

### P5: 数据库操作

- Prisma 基础（查询、创建、更新、删除）
- 理解数据模型关系
- 实现文章 CRUD

### P6: 前后端联调

- 前端调用后端 API
- 使用 TanStack Query 管理数据
- 错误处理和加载状态

### P7: 功能完善

- 实现完整的文章管理功能
- 添加认证系统
- 部署上线

---

## 如何使用本文档

### 学习流程

1. **按顺序完成每个阶段**
   - 不要跳过，即使觉得简单

2. **每个任务都要动手实践**
   - 不只是阅读，要自己写代码
   - 遇到问题先尝试解决，解决不了再问

3. **记录学习笔记**
   - 遇到的问题和解决方案
   - 新学到的概念
   - 自己的理解和疑问

4. **遇到问题时**
   - 先看错误信息，尝试理解
   - 搜索错误信息（Google/Stack Overflow）
   - 查阅官方文档
   - 最后再问 AI 助手

### 与 AI 助手协作

**好的提问方式**：

```
我在完成 P2 的任务 2.3，创建 Counter 组件时遇到了问题。
错误信息：TypeError: setCount is not a function
我的代码是：[贴代码]
我尝试了：[你的尝试]
```

**不好的提问方式**：

```
代码报错了，帮我看看
```

---

## 开始学习

**准备好了吗？从 P0 开始吧！**

如果 P0 的任务你已经完成了（项目能跑起来），可以直接从 P1 开始学习 TypeScript。

有任何问题随时问我，我会根据你的进度提供指导。加油！🚀
